[package]
name = "platform_wallet_near"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-sdk = "5.1"

[profile.release]
codegen-units = 1
opt-level = "z"
lto = true
overflow-checks = true
